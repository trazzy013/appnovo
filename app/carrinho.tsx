import { Alert, Image, Pressable, ScrollView, StyleSheet, Text, View } from 'react-native';
import { router } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';
import { useCart } from './src/context/CartContext';
import { useTheme } from './src/context/ThemeContext';
export default function Carrinho() { const { items, total, count, changeQuantity, remove, clear } = useCart(); const { theme } = useTheme(); if (!items.length) return <View style={[styles.empty, { backgroundColor: theme.background }]}><View style={[styles.emptyIcon, { backgroundColor: theme.accentSoft }]}><Ionicons name="bag-outline" size={31} color={theme.primary}/></View><Text style={[styles.emptyTitle, { color: theme.text }]}>Seu carrinho está vazio</Text><Text style={[styles.emptyText, { color: theme.textMuted }]}>Salve seus produtos favoritos para encontrá-los aqui.</Text><Pressable style={[styles.continue, { backgroundColor: theme.primary }]} onPress={() => router.push('/produtos')}><Text style={styles.continueText}>Explorar produtos</Text></Pressable></View>; return <ScrollView style={{ backgroundColor: theme.background }} contentContainerStyle={styles.content}><View style={styles.header}><View><Text style={[styles.eyebrow, { color: theme.primary }]}>MEUS PRODUTOS</Text><Text style={[styles.title, { color: theme.text }]}>Carrinho ({count})</Text></View><Pressable onPress={() => Alert.alert('Limpar carrinho', 'Deseja remover todos os produtos?', [{ text: 'Cancelar', style: 'cancel' }, { text: 'Limpar', style: 'destructive', onPress: clear }])}><Text style={[styles.clear, { color: theme.primary }]}>Limpar</Text></Pressable></View>{items.map(item => <View key={item.id} style={[styles.item, { backgroundColor: theme.surface, borderColor: theme.border }]}><Image source={{ uri: item.image }} style={styles.image}/><View style={styles.itemInfo}><Text style={[styles.itemName, { color: theme.text }]}>{item.name}</Text><Text style={[styles.itemPrice, { color: theme.primary }]}>R$ {item.price.toFixed(2).replace('.', ',')}</Text><View style={styles.quantity}><Pressable onPress={() => changeQuantity(item.id, item.quantity - 1)} style={[styles.quantityButton, { backgroundColor: theme.accentSoft }]}><Ionicons name="remove" size={16} color={theme.primary}/></Pressable><Text style={[styles.quantityText, { color: theme.text }]}>{item.quantity}</Text><Pressable onPress={() => changeQuantity(item.id, item.quantity + 1)} style={[styles.quantityButton, { backgroundColor: theme.accentSoft }]}><Ionicons name="add" size={16} color={theme.primary}/></Pressable></View></View><Pressable onPress={() => remove(item.id)}><Ionicons name="trash-outline" size={19} color={theme.textMuted}/></Pressable></View>)}<View style={[styles.summary, { borderTopColor: theme.border }]}><View style={styles.totalRow}><Text style={[styles.totalLabel, { color: theme.textMuted }]}>Total</Text><Text style={[styles.total, { color: theme.text }]}>R$ {total.toFixed(2).replace('.', ',')}</Text></View><Pressable style={[styles.finish, { backgroundColor: theme.primary }]} onPress={() => Alert.alert('Pedido salvo', 'Seu pedido foi preparado para finalizar.')}><Text style={styles.finishText}>Continuar compra</Text><Ionicons name="arrow-forward" color="#fff" size={18}/></Pressable></View></ScrollView> }
const styles = StyleSheet.create({ empty: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: 35 }, emptyIcon: { width: 70, height: 70, borderRadius: 23, alignItems: 'center', justifyContent: 'center' }, emptyTitle: { fontSize: 21, fontWeight: '800', marginTop: 19 }, emptyText: { textAlign: 'center', lineHeight: 21, marginTop: 8, maxWidth: 270 }, continue: { marginTop: 24, borderRadius: 13, height: 48, paddingHorizontal: 20, justifyContent: 'center' }, continueText: { color: '#fff', fontWeight: '800' }, content: { padding: 22, paddingBottom: 40 }, header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: 24 }, eyebrow: { fontSize: 11, letterSpacing: 1.2, fontWeight: '800' }, title: { fontSize: 27, fontWeight: '800', marginTop: 6 }, clear: { fontWeight: '800', fontSize: 13 }, item: { borderWidth: 1, borderRadius: 17, padding: 10, flexDirection: 'row', gap: 11, marginBottom: 11 }, image: { width: 72, height: 72, borderRadius: 12, backgroundColor: '#F8EDF1' }, itemInfo: { flex: 1 }, itemName: { fontWeight: '800', fontSize: 14 }, itemPrice: { fontWeight: '800', fontSize: 13, marginTop: 4 }, quantity: { flexDirection: 'row', gap: 10, alignItems: 'center', marginTop: 9 }, quantityButton: { width: 25, height: 25, borderRadius: 8, alignItems: 'center', justifyContent: 'center' }, quantityText: { fontWeight: '800', minWidth: 13, textAlign: 'center' }, summary: { borderTopWidth: 1, marginTop: 18, paddingTop: 19 }, totalRow: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, totalLabel: { fontSize: 15, fontWeight: '700' }, total: { fontSize: 21, fontWeight: '800' }, finish: { height: 53, borderRadius: 14, marginTop: 18, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 9 }, finishText: { color: '#fff', fontWeight: '800', fontSize: 15 } });
